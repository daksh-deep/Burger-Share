<!DOCTYPE html>
<html lang="en">
<head>
    <!-- Character Encoding and Viewport Settings -->
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <!-- Page Title -->
    <title>404 | Burger Share</title>

    <!-- External Stylesheet Link -->
    <link rel="stylesheet" href="/CSS/global.css">

    <!-- TailwindCSS CDN for utility classes -->
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-black text-white">
    <!-- Centered Content Container -->
    <div class="flex justify-center items-center min-h-screen w-full p-4">
        <div class="w-full max-w-3xl">

            <!-- Navigation Bar (Include Partial for Reusability) -->
            <%- include('partials/back_nav') %>

            <!-- Main Error Message Section -->
            <div class="text-center space-y-6">
                <!-- Error Title -->
                <div class="space-y-2">
                    <h2 class="text-3xl md:text-5xl">
                        Page Not Found
                    </h2>
                </div>

                <!-- Optional Error Message -->
                <div class="space-y-4">
                    <% if (errorMessage) { %>
                        <p class="text-xl md:text-2xl text-gray-400">
                            <%= errorMessage %>
                        </p>
                    <% } %>
                </div>

                <!-- 404 Visual Representation -->
                <div class="py-8">
                    <div class="text-8xl md:text-9xl font-bold opacity-10 select-none">
                        404
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
